- content_for :head do
  %title #{resource.title}
  %meta{ :name => "keywords", :content => "#{resource.keywords}" }
  = resource.meta.html_safe

= markdown( text_cut( resource.left ) )

- content_for :right do
  = markdown( text_cut( resource.right ) )
